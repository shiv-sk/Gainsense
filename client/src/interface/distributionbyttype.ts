export interface DistributionByType {
    investment_type: string,
    total_invested_per_type: number,
    Avg_returns_per_type: number,
    profit_loss_per_type: number
}

export interface DistributionByTypeTable {
    investment_type: string,
    Avg_returns_per_type: number,
    profit_loss_per_type: number
}